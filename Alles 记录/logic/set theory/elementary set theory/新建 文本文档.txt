
如果实数集的一个子集A在<序下同构于一个序数，那么A一定可数：

第一个证明：
假如不可数，令A_n=[n, n+1]∩A，那么一定有个A_n不可数，否则矛盾。由于A_n良序，所以A一定有一个与ω_1同构的有界子集A_n，令b = sup B，则b昰B的凝聚点（B的任何邻域中都有一不等于b的A中的点），因此存在B\{b}中的可数序列(b_n)收敛到b，对应的可数序数序列就成了ω_1的共尾，与它昰正则基数矛盾
这个序列的选取不用任何形式的选择公理，因为它昰良序的，很容易构造出满足需要的序列

第二个证明：
假设R有一个在其自然序＜下的不可数的良序子集A, 令α是( A, ＜)的序型，α必然大于ω_1。固定一个A的枚举{ a_β:β∈α}，对每个β∈ω_1，a_β和a_β+1之间必然存在一个有理数r_β。并且每个r_β互不相同，这样就至少有ω_1那么多个有理数，矛盾。
有理数有可数的序型，可以规定这中间选一个序型最小的有理数。

或：对每个β∈ω_1，区间(a_β, a_β+1)∩Q非空并且对每个β，这样的区间两两不相交，而这些区间都是Q的子集，这意味着Q包含了一个至少是ω_1那么大的子集，矛盾。（存在ω_1到这个子集的单射得靠选取。选有理数，不需要选择公理，事先固定一个有理数的枚举就行）



