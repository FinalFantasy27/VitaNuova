开算 0:24:02
因为一阶逻辑里面根本没有函数，只有谓词，用 P(m,n) 表示“输入m，输出n”的函数

开算 0:24:31
所以MLTT里面比较也当然应该比较 N -> N -> Type 这样的函数，而不是 N -> N

不会算 0:48:53
Trebor  
因为一阶逻辑里面根本没有函数，只有谓词，用 P(m,n) 表示“输入m，输出n”的函数
但是也可以有函数

开算 0:55:13
Alias Qli  
但是也可以有函数
@不会算 就算有了那也远远弱于原始递归函数

开算 0:55:33
因为原始递归函数需要一个递归子，那是高阶函数

开算 0:55:49
除非你给每个原始递归函数都塞一个primitive到理论里

开算 0:55:57
（PRA就是这么做的）

